# ARCHITECTURE.md

## Overview

[High-level description of the system's architecture, in plain language
before any diagram or table]

## Architecture Diagram

<!-- A text-based diagram (e.g. a simple ASCII box diagram or a Mermaid
     diagram in a fenced code block) showing the major modules and how
     data/control flows between them. -->

```
(diagram here)
```

## Layers / Modules

| Module | Responsibility | Depends on |
|---|---|---|

## Data Flow

[Describe how data moves through the system for the 1-2 most important
flows — e.g. "a request enters at X, gets validated by Y, persisted by
Z." Use the actual flow as it exists post-refactor, not an idealized
version.]

## Key Architectural Decisions

[Anything non-obvious about why the system is structured this way —
especially decisions that were validated or explained during the audit
and refactor stages]

## Dependencies

See [DEPENDENCY_MAP.md](./DEPENDENCY_MAP.md) for the full graph.
