# COMPONENT_GUIDE.md

> One entry per UI component (or equivalent unit, if this isn't a
> frontend project — adapt "component" to whatever the codebase's
> actual unit of composition is).

## [ComponentName]

- **Location:**
- **Purpose:**
- **Inputs (props/parameters):**
- **Outputs (events/callbacks/return values):**
- **Dependencies:**
- **Side effects:** <!-- e.g. network calls, global state mutation -->
- **Example usage:**

```
(code example)
```

- **Common failure modes:** <!-- e.g. "throws if X prop is undefined,"
                                 "silently no-ops if Y context missing" -->
